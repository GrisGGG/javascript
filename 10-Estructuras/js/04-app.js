const dinero = 500;
const totalAPagar = 200;
const tarjeta = true;

if (dinero > totalAPagar) {
  // < menor que > mayor que
  console.log("si ponedmos pagar");
} else if (tarjeta) {
  console.log("pagar con tarjeta");
} else {
  console.log("no podemos pagarr");
}
