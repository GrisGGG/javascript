const producto = {
  nombre: "Monitor de 20 pulgadas",
  precio: 200,
  disponible: true,
};
console.log(Object.keys(producto)); //nombre, precio, disponible

console.log(Object.values(producto)); // valores

console.log(Object.entrie(producto)); // imprime todo
