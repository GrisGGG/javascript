const nombre = "Monitor 20 pulgadas";
const precio = 300;
const disponible = true;

//Un objeto agrupa todo en una sola variables
//Object lliteral
const producto = {
  //keys del objeto
  nombre: "Monitor de 20 pulgadas",
  precio: 200,
  disponible: true,
};

console.log(producto);
