//accedeer a las keys

const producto = {
  //keys del objeto
  nombre: "Monitor de 20 pulgadas",
  precio: 200,
  disponible: true,
};
//sintacxis de punto
console.log(producto.nombre);
console.log(producto["nombre"]);
